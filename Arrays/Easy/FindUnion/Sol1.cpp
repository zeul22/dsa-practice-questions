#include <bits/stdc++.h>
using namespace std;
void Union(int *arr, int size, int *arr2, int size2);
int main() {}

void Union(int *arr, int size, int *arr2, int size2)
{
    int *arr3 = (int *)malloc((size + size2) * sizeof(int));
    for (int i = 0; i < size; i++)
    {
        for (int j = 0; j < size2; i++)
        {
            /* code */
        }
    }

    int i = 0;
    int j = 0;
    while (i < size && j < size2)
    {
    }
}